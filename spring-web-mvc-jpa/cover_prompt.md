# Cover Design Record — 「React에서 Spring으로」

- **Slug:** spring-web-mvc-jpa
- **Title:** React에서 Spring으로
- **Subtitle:** 백엔드를 처음 만드는 프런트엔드 개발자의 안내서
- **Author:** Toby-AI
- **Genre:** tech-book
- **Output:** `spring-web-mvc-jpa/cover.png` (1600×2560, 1.6:1)

## 콘셉트 3안

1. **(채택) 코드 에디터 × 여정 은유 — 미니멀 타이포그래피**
   딥 네이비 배경에 `React ──▶ Spring`을 모노스페이스로 박아 "건너간다"는 이동의 은유를
   한 줄로 표현. 화살표/연결선에만 Spring 그린(#6DB33F) 액센트. 대형 한글 제목(Gmarket Bold)으로
   썸네일 가독성 확보. 정직·정공법 tone과 정확히 호응.
2. 다리(bridge) 일러스트형 — 두 강변(프런트/백엔드)을 잇는 다리. 과한 일러스트는 썸네일에서 죽고 오래 못 감 → 보류.
3. 어노테이션 마법 은유 — `@`기호 모티프. 개성 있지만 "입문서" 메시지가 흐려짐 → 보류.

## 채택안 디자인 의도

- **배경:** 딥 네이비 그라데이션 (#0d1b2a → #14253a). tech-book의 차분한 깊이.
- **여정 라인:** 상단에 `React` → (Spring 그린 화살표) → `Spring`. 독자의 출발점에서 목적지로의 이동.
- **제목:** Gmarket Sans Bold, 한글 대형 2줄. 고대비 화이트.
- **부제:** Apple SD Gothic Neo, 회색조.
- **액센트:** Spring 그린(#6DB33F)을 화살표·구분선·코드 토큰에만 절제 사용.
- **하단:** 저자 `Toby-AI` + Spring Boot 3.x / Security 6.x / Java 17·21 빌드 태그(모노).

## 생성 방법

- 이미지 생성 MCP/API 미연결 → ImageMagick 타이포그래피로 제작 (tech-book에 적합한 폴백이자 정답).
- Fonts: `~/Library/Fonts/GmarketSansBold.otf` (제목), `/System/Library/Fonts/AppleSDGothicNeo.ttc` (부제),
  `~/Library/Fonts/JetBrainsMonoNerdFontMono-Regular.ttf` (코드 토큰).

## 재생성 메모

- A/B 시 안2(다리 일러스트)는 외부 이미지 생성 모델이 연결됐을 때 시도할 것.
- 그린 액센트 비중을 더 줄이고 싶으면 화살표만 그린, 나머지 화이트로.
