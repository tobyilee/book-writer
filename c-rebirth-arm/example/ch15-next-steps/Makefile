# ch15-next-steps — 빌드 타깃 없음. 학습 안내 디렉토리.
#
# 다음 네 갈래 길(인터럽트·MMU·컨텍스트 스위치·시스템 콜)은 본 책의
# 범위를 넘는다. README.md 에 권장 학습 경로와 참고 코드베이스가 정리돼 있다.

.PHONY: help all run clean

help all run clean:
	@echo "================================================================"
	@echo " ch15-next-steps — 빌드 타깃이 없는 학습 안내 디렉토리"
	@echo "================================================================"
	@echo ""
	@echo " 이 디렉토리는 코드를 빌드하지 않는다. README.md 를 읽고"
	@echo " 다음 네 단계의 학습 경로를 참고하자."
	@echo ""
	@echo "   1. 인터럽트 (GIC + 예외 벡터 테이블)"
	@echo "   2. MMU (페이지 테이블)"
	@echo "   3. 컨텍스트 스위치 (태스크 스케줄링)"
	@echo "   4. 시스템 콜 (EL0 ↔ EL1 경계)"
	@echo ""
	@echo " 권장 코드베이스: s-matyukevich/raspberry-pi-os, bztsrc/raspi3-tutorial"
	@echo "================================================================"
