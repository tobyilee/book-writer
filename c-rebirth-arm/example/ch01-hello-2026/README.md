# ch01-hello-2026 — 같은 인사, 두 시대

같은 `hello.c` 한 파일을 `-std=c89`로 한 번, `-std=c23`으로 한 번 빌드해본다. 컴파일러가 정의해주는 `__STDC_VERSION__` 매크로가 표준마다 달라지는 모습을 직접 눈으로 본다. `make both`로 두 빌드를 한 번에 돌리고, 두 실행 파일이 같은 소스에서 어떻게 다른 인사를 내놓는지 비교해보자. 이 예제가 통과한다면, 책 1장이 보여주려는 "C는 멈춰 있지 않다"는 명제를 손으로 만진 셈이다.
