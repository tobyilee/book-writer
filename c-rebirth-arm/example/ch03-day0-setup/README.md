# ch03-day0-setup — Apple Silicon Day 0

Mac을 켜고 한 시간 안에 C 개발 환경이 준비되어 있는가? `make check`로 호스트에 깔려 있는 도구 목록과 버전을 한 화면에 본다. `make run`은 `triple.c`를 빌드해서 실행하고, 지금 이 컴파일러가 자기를 어떻게 부르고 있는지(`__aarch64__`, `__APPLE__`, `__BYTE_ORDER__` 등)를 화면에 출력한다. 베어메탈 챕터로 옮겨가기 전에, 호스트 단계에서부터 "타깃이 누구인가"를 손에 익혀두자.
